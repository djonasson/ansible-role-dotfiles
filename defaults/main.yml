---
repo: "https://github.com/djonasson/dotfiles.git"
dest: "{{ lookup('env', 'XDG_DATA_HOME') | default(ansible_user_dir + '/.local/share', true) }}/dotfiles"
version: main
accept_hostkey: false

installation_dir: "~"
